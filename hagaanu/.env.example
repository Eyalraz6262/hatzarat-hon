# Google Maps key for Android. See README.md → "מפתח Google Maps ל-Android".
# iOS uses Apple Maps and needs no key.
# Without this the Android map renders blank; everything else still works.
GOOGLE_MAPS_ANDROID_API_KEY=
